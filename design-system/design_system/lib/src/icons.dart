import 'package:flutter/material.dart';

const Icon defaultInputIcon = Icon(Icons.input);

// Inbound form input icons
const Icon supplierIcon = Icon(Icons.factory_sharp);
const Icon editIcon = Icon(Icons.edit);
const Icon inventoryIcon = Icon(Icons.inventory_sharp);
const Icon identificationIcon = Icon(Icons.tag);


